{
  "schemaVersion": "tutti.app.cli.v1",
  "scope": "automation",
  "description": "Create and schedule automation tasks.",
  "documentation": {
    "file": "COMMANDS.md"
  },
  "commands": [
    {
      "path": ["list"],
      "summary": "List automation tasks",
      "description": "List automation task definitions.",
      "output": {
        "defaultMode": "table",
        "json": true,
        "table": {
          "columns": [
            { "key": "id", "label": "ID" },
            { "key": "name", "label": "Name" },
            { "key": "enabled", "label": "Enabled" },
            { "key": "schedule", "label": "Schedule" },
            { "key": "next-run", "label": "Next run" },
            { "key": "active-run", "label": "Active run" },
            { "key": "unreviewed", "label": "Unreviewed" }
          ]
        }
      },
      "handler": {
        "kind": "http",
        "method": "POST",
        "path": "/tutti/cli/list",
        "timeoutMs": 30000
      }
    },
    {
      "path": ["get"],
      "summary": "Get an automation task",
      "description": "Get one automation task definition by id or exact name.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "automation-id": {
            "type": "string",
            "description": "Automation task id to read."
          },
          "name": {
            "type": "string",
            "description": "Exact automation task name to read when automation-id is omitted."
          }
        }
      },
      "output": {
        "defaultMode": "json",
        "json": true
      },
      "handler": {
        "kind": "http",
        "method": "POST",
        "path": "/tutti/cli/get",
        "timeoutMs": 30000
      }
    },
    {
      "path": ["create"],
      "summary": "Create an automation task",
      "description": "Create an automation task definition.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Automation task name."
          },
          "prompt": {
            "type": "string",
            "description": "Prompt to send to the runner."
          },
          "cwd": {
            "type": "string",
            "description": "Working directory. Defaults to the workspace root."
          },
          "enabled": {
            "type": "boolean",
            "description": "Whether scheduled runs are enabled."
          },
          "schedule-type": {
            "type": "string",
            "description": "Schedule type. Use daily with --time-of-day, weekly with --days-of-week and --time-of-day, or cron with --cron.",
            "enum": ["interval", "daily", "weekly", "cron"],
            "default": "daily"
          },
          "interval-minutes": {
            "type": "integer",
            "description": "Interval schedule in minutes. Use only with --schedule-type interval."
          },
          "time-of-day": {
            "type": "string",
            "description": "Daily or weekly run time in HH:MM. Use only with --schedule-type daily or weekly."
          },
          "days-of-week": {
            "type": "string",
            "description": "Comma-separated ISO weekdays for weekly schedules, 1=Monday through 7=Sunday. Use only with --schedule-type weekly."
          },
          "cron": {
            "type": "string",
            "description": "Five-field cron expression for cron schedules. Use only with --schedule-type cron."
          },
          "concurrency": {
            "type": "string",
            "description": "Concurrent run policy: skip, queue, or replace.",
            "enum": ["skip", "queue", "replace"],
            "default": "queue"
          },
          "agent-id": {
            "type": "string",
            "description": "Exact Agent Target id. Omit to use the host default Agent. Discover values with `tutti agent list --json`."
          },
          "provider": {
            "type": "string",
            "description": "Deprecated compatibility input for old hosts. Accepted only when the provider maps to exactly one Agent Target in the full catalog; prefer --agent-id."
          },
          "model": {
            "type": "string",
            "description": "Runner model id. Optional when the selected Agent supports its runtime default. Discover options with `tutti agent composer-options --agent-id <agent-target-id> --json`."
          },
          "reasoning-effort": {
            "type": "string",
            "description": "Runner reasoning effort."
          },
          "permission-mode": {
            "type": "string",
            "description": "Runner permission mode."
          },
          "runner-args": {
            "type": "string",
            "description": "Additional runner args, parsed with shell-style quoting."
          },
          "env": {
            "type": "string",
            "description": "Comma-separated KEY=VALUE environment variables."
          }
        },
        "required": ["name", "prompt"]
      },
      "output": {
        "defaultMode": "json",
        "json": true
      },
      "handler": {
        "kind": "http",
        "method": "POST",
        "path": "/tutti/cli/create",
        "timeoutMs": 30000
      }
    },
    {
      "path": ["update"],
      "summary": "Update an automation task",
      "description": "Update one automation task definition by id. Omitted fields keep their current values.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "automation-id": {
            "type": "string",
            "description": "Automation task id to update."
          },
          "name": {
            "type": "string",
            "description": "Automation task name."
          },
          "prompt": {
            "type": "string",
            "description": "Prompt to send to the runner."
          },
          "cwd": {
            "type": "string",
            "description": "Working directory."
          },
          "enabled": {
            "type": "boolean",
            "description": "Whether scheduled runs are enabled."
          },
          "schedule-type": {
            "type": "string",
            "description": "Schedule type. Use daily with --time-of-day, weekly with --days-of-week and --time-of-day, or cron with --cron.",
            "enum": ["interval", "daily", "weekly", "cron"]
          },
          "interval-minutes": {
            "type": "integer",
            "description": "Interval schedule in minutes. Use only with --schedule-type interval."
          },
          "time-of-day": {
            "type": "string",
            "description": "Daily or weekly run time in HH:MM. Use only with --schedule-type daily or weekly."
          },
          "days-of-week": {
            "type": "string",
            "description": "Comma-separated ISO weekdays for weekly schedules, 1=Monday through 7=Sunday. Use only with --schedule-type weekly."
          },
          "cron": {
            "type": "string",
            "description": "Five-field cron expression for cron schedules. Use only with --schedule-type cron."
          },
          "concurrency": {
            "type": "string",
            "description": "Concurrent run policy: skip, queue, or replace.",
            "enum": ["skip", "queue", "replace"]
          },
          "agent-id": {
            "type": "string",
            "description": "Exact Agent Target id. Discover values with `tutti agent list --json`."
          },
          "provider": {
            "type": "string",
            "description": "Deprecated compatibility input for old hosts. Accepted only when the provider maps to exactly one Agent Target in the full catalog; prefer --agent-id."
          },
          "model": {
            "type": "string",
            "description": "Runner model id. Optional when the selected Agent supports its runtime default. Discover options with `tutti agent composer-options --agent-id <agent-target-id> --json`."
          },
          "reasoning-effort": {
            "type": "string",
            "description": "Runner reasoning effort."
          },
          "permission-mode": {
            "type": "string",
            "description": "Runner permission mode."
          },
          "runner-args": {
            "type": "string",
            "description": "Additional runner args, parsed with shell-style quoting."
          },
          "env": {
            "type": "string",
            "description": "Comma-separated KEY=VALUE environment variables."
          }
        },
        "required": ["automation-id"]
      },
      "output": {
        "defaultMode": "json",
        "json": true
      },
      "handler": {
        "kind": "http",
        "method": "POST",
        "path": "/tutti/cli/update",
        "timeoutMs": 30000
      }
    },
    {
      "path": ["delete"],
      "summary": "Delete an automation task",
      "description": "Delete one automation task definition and its run history by id.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "automation-id": {
            "type": "string",
            "description": "Automation task id to delete."
          }
        },
        "required": ["automation-id"]
      },
      "output": {
        "defaultMode": "json",
        "json": true
      },
      "handler": {
        "kind": "http",
        "method": "POST",
        "path": "/tutti/cli/delete",
        "timeoutMs": 30000
      }
    },
    {
      "path": ["run"],
      "summary": "Run an automation task",
      "description": "Trigger one automation task immediately by id or exact name.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "automation-id": {
            "type": "string",
            "description": "Automation task id to run."
          },
          "name": {
            "type": "string",
            "description": "Exact automation task name to run when automation-id is omitted."
          }
        }
      },
      "output": {
        "defaultMode": "json",
        "json": true
      },
      "handler": {
        "kind": "http",
        "method": "POST",
        "path": "/tutti/cli/run",
        "timeoutMs": 30000
      }
    },
    {
      "path": ["runs"],
      "summary": "List automation task runs",
      "description": "List recent automation task runs, optionally filtered by automation task id.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "automation-id": {
            "type": "string",
            "description": "Automation task id to filter runs."
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of runs to return."
          }
        }
      },
      "output": {
        "defaultMode": "table",
        "json": true,
        "table": {
          "columns": [
            { "key": "id", "label": "ID" },
            { "key": "automation-id", "label": "Task" },
            { "key": "agent-id", "label": "Agent" },
            { "key": "run-status", "label": "Run status" },
            { "key": "task-status", "label": "Task status" },
            { "key": "trigger", "label": "Trigger" },
            { "key": "queued", "label": "Queued" },
            { "key": "finished", "label": "Finished" }
          ]
        }
      },
      "handler": {
        "kind": "http",
        "method": "POST",
        "path": "/tutti/cli/runs",
        "timeoutMs": 30000
      }
    },
    {
      "path": ["complete-run"],
      "summary": "Submit an automation task run status",
      "description": "Submit the final structured status for the current automation task run.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "run-id": {
            "type": "string",
            "description": "Automation task run id to complete."
          },
          "status": {
            "type": "string",
            "description": "Automation task result status: success, fail, or skip."
          }
        },
        "required": ["run-id", "status"]
      },
      "output": {
        "defaultMode": "json",
        "json": true
      },
      "handler": {
        "kind": "http",
        "method": "POST",
        "path": "/tutti/cli/complete-run",
        "timeoutMs": 30000
      }
    }
  ]
}
